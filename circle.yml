general:
  branches:
    only:
      - cli

machine:
  environment:
    PATH: "${HOME}/${CIRCLE_PROJECT_REPONAME}/bin:${HOME}/${CIRCLE_PROJECT_REPONAME}/node_modules/.bin:${PATH}"
  node:
    version: 0.12.7

dependencies:
  pre:
    - npm install -g bower
  override:
    - npm install
    - bower install
test:
  override:
    - ember test
