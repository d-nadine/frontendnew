general:
  branches:
    only:
      - master
      - cli
machine:
  node:
    version: 0.12.7
  post:
    - export PATH="./node_modules/.bin:$PATH"

dependencies:
  pre:
    - npm install -g bower
  override:
    - npm install
    - bower install
test:
  override:
    - e mber test
