{{! this template exists to wrap the with call and else}}
{{! the with block is not entered if nextTask is null/undefined}}
{{! this means the next_task control will never show the empty state}}
{{#if nextTask}}
  {{#with nextTask}}
    {{control 'next_task' this}}
  {{/with}}
{{else}}
  {{control 'next_task'}}
{{/if}}
