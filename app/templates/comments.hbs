<div class="comments">
  {{#if comments.length}}
    <ul class="comments-list unstyled">
      {{#each comments}}
        <li class="comment">
          {{#linkTo user user}}{{avatar user}}{{/linkTo}}

          <div class="text">
            {{text}}
          </div>
        </li>
      {{/each}}
    </ul>
  {{/if}}

  {{view view.commentTextArea}}
</div>
