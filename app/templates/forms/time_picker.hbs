{{#if view.disabled}}
  <i class="icon-time"></i>
{{else}}
  <div class="btn-group">
    <a class="btn btn-link" href="#" {{action showTimes target="view"}} tabindex="-1">
      <i class="icon-time"></i>
    </a>
  </div>
{{/if}}
<span class="text">{{view.leader}}</span>
{{view view.timeField}}

