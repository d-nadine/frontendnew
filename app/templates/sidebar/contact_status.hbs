{{#if isEditing}}
  <div>{{view view.statusSelect}}</div>
{{else}}
  <div class="pull-right">
    <i class="ss-standard ss-write" {{action "toggleEditor" target=view bubbles=false}}></i>
  </div>
  <p>{{controller.contactStatus.name}}</p>
{{/if}}
