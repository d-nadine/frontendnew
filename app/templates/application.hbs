{{nav-bar currentUser=currentUser transitionToList="transitionToList" today=today toggleNotifications="toggleNotifications" logOut="logOut" notificationCount=notificationCount currentPath=currentPath}}

{{outlet}}
