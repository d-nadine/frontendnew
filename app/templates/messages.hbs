{{#if hasCheckedContent}}
  {{render 'messages/bulk_actions' checkedContent}}
{{else}}
  {{#if createEmail}}
    {{render 'messages/create_email'}}
  {{else}}
    {{#with selectedContent}}
      {{render 'messages/email_panel' this}}
    {{/with}}
  {{/if}}
{{/if}}
