<div class="message-item-actions">

  <div class="pull-right">
    {{#if canArchive}}
      <a {{action "archive" this true bubbles=false}} href="#" class="btn-icon archive" title="Archive email">
        <i class='ss-standard ss-box'></i>
      </a>
    {{/if}}
    <a {{action "delete" this true bubbles=false}} href="#" class="btn-icon delete on-sidebar">
      <i class='ss-symbolicons-block ss-trash'></i>
    </a>
    {{x-check class="check-box" checked=isChecked disabled=disabled sendCheck='checkMessageItem'}}
  </div>
  <div class="message-item-labels">
    {{#if hasLead}}
      <span class='badge badge-success'>Tracked</span>
    {{/if}}

    {{#if fromUser}}
      <span class='badge badge-warning'>User</span>
    {{/if}}

    {{#if hasDeal}}
      {{partial 'deals/deal_badge'}}
    {{/if}}

    {{#if todos.length}}
      <i class="ss-standard ss-check"></i>
    {{/if}}

    {{#if calls.length}}
      <i class="ss-standard ss-phone"></i>
    {{/if}}

    {{#if meetings.length}}
      <i class="ss-standard ss-calendar"></i>
    {{/if}}

    {{#if hasAttachments}}
      <i class="ss-standard ss-attach"></i>
    {{/if}}

    {{#if hasComments}}
      <i class="ss-symbolicons-block ss-chat"></i>
    {{/if}}
  </div>
</div>
