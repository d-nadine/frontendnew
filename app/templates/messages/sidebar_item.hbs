{{view view.checker}}

<div {{bindAttr class="isSelectable :content"}} {{action selectContent content}}>
  {{#if hasAttachments}}
    <i class="icon-tag pull-right"></i>
  {{/if}}

  {{yield}}

  <div class="footer">
    <a {{action deleteItem this target="controllers.messages"}} href="#" class="delete">
      <i class='icon-trash'></i>
    </a>

    {{time timestamp class="muted upcase pull-right"}}
  </div>
</div>
