<div class="members">
  {{#if contacts.length}}
    <a href="#">{{membersText}}</a>
    {{#each truncatedContacts}}
      <div class="contact">
        {{avatar this size=small class="img-polaroid"}}
        {{name}}
      </div>
    {{/each}}
    {{#if hasMoreContacts}}
      <strong>...</strong>
    {{/if}}
  {{else}}
    <label> No Members</label>
  {{/if}}
</div>
