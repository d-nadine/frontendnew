{{#if content}}
  {{#if hasPreviousEmails}}
    <a  class="btn btn-large btn-full-width" {{action selectObject mostRecentEmail}}>
      Go to Most Recent Email<i class="icon-arrow-right pull-right"></i>
    </a>
  {{/if}}

  {{view view.EmailsListView}}

  {{#if remainingContent}}
    <div id="remaining-emails">
      <a class="btn btn-large btn-full-width" {{action showMore target="controller"}}>
        <i class="icon-plus"></i>{{remainingContent.length}} Email(s) Remaining: Show More
      </a>
    </div>
  {{/if}}
{{else}}
  <div class="block">
    <h4>No Email Selected</h4>
  </div>
{{/if}}
