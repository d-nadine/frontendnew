<div class="btn-toolbar">
  <div class="btn-group">
    <a href="#" class="btn btn-success">
      New Message <i class="icon-plus icon-white"></i>
    </a>
  </div>
  {{#if selectedMail.length}}
  <div class="btn-group">
    <a class="btn btn-danger" href="#">
      <i class="icon-trash icon-large icon-white"></i> Delete
    </a>
  </div>
  {{/if}}
</div>
{{outlet}}
{{view view.arrow}}
