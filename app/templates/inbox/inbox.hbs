<div class="btn-toolbar">
  <div class="btn-group">
    <a href="#" class="btn btn-success" {{action showNewMailDialog target="view"}}>
      New Message <i class="icon-plus icon-white"></i>
    </a>
  </div>
  {{#if selectedMail.length}}
  <div class="btn-group">
    <a class="btn btn-danger" href="#">
      <i class="icon-trash icon-large"></i> Delet<ei
    </a>
  </div>
  {{/if}}
</div>
<hr/>
{{outlet}}
{{view view.arrow}}
