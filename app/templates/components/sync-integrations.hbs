<ul>
  <li {{action "showIntegration" "intercom"}}>
    <img src="https://res.cloudinary.com/radium/image/upload/c_scale,h_20,w_20/v1450520164/integrations/intercomio2.png" alt="intercom"/>
    Add new leads from intercom.  Requires an intercom account.
  </li>
  {{#if showIntercomConfig}}
    <div class="integration-config intercom">
      <h4 class="underline-heading"><img src="https://res.cloudinary.com/radium/image/upload/c_scale,h_20,w_20/v1450520164/integrations/intercomio2.png" alt="intercom"/> Intercom Settings</h4>
      <span>Your App ID is available on the <a href="https://app.intercom.io/a/apps/_/rails-integrations/api-keys" target="_blank">API Keys</a> page on Intercom, accessible from your Integration settings.</span>
      {{#unless intercomSet}}
        <div class="control-box">
          <input type="text" placeholder="APP ID" class="field app_id"/>
        </div>
        <div class="control-box">
          <input type="text" placeholder="API KEY" class="field api_key"/>
        </div>
      {{/unless}}
      <div class="list-integrations">
        <strong>When a contact is added to Radium which list(s) do you want thems added to?</strong>
        <div class="sidebar-list">
          <i class="ss-standard ss-list"></i>
          {{list-autosuggest destination=integrationLists addList="addList" removeList="removeList" placeholder="Add to lists"}}
        </div>
      </div>
      {{#unless intercomSet}}
        <div>
          <button class="btn" {{action "hideIntegration" "intercom"}}>Cancel</button>
          <button class="btn btn-primary" {{action "updateIntegration" "intercom"}}>Connect</button>
        </div>
      {{else}}
        <div>
          <button class="btn" {{action "update" "intercom"}}>Update Configuration</button>
          <button class="btn" {{action "disconnect" "intercom"}} {{bind-attr disabled=isDeleting}}>Disconnect</button>
        </div>
      {{/unless}}

    </div>
  {{/if}}
</ul>
{{partial 'modal'}}
