{{async-autosuggest destination=contacts placeholder="Add contacts to deal" queryParameters=queryParameters filterResults=filterResults addSelection="addContact" removeSelection="removeContact" itemAction="itemAction"}}
