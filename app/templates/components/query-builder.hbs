<div class="variables">
  {{#each selectedQuery in potentialQueries}}
    {{x-query query=selectedQuery parent=this index=_view.contentIndex users=users}}
  {{/each}}
</div>
<div class="combobox-container">
  {{autocomplete-textbox source=fields value=value class="field" queryKey="displayName" placeholder="Filter By" actionOnly=true action="addPotentialQuery"}}
</div>
{{#if saveAvailable}}
  <div class="save-dynamic-list">
    <button class="btn" {{action "persistCustomQuery"}}>
      {{#if isSaved}}
        Update Dynamic List
      {{else}}
        Save Dynamic List
      {{/if}}
    </button>
  </div>
{{/if}}