{{#each files}}
  <div class="upload-file">
    {{#unless isUploading}}
      <a href="{{unbound attachment.url}}" target="_new" class="uploaded-file">{{name}} ({{size}})</a>
    {{else}}
      {{name}} ({{size}})
    {{/unless}}
    <button type="button" {{action removeFile this}} class="close">
      {{#unless isUploading}}
        <i class="ss-standard ss-delete"></i>
      {{else}}
        {{partial 'is_uploading'}}
      {{/unless}}
    </button>
  </div>
{{/each}}
