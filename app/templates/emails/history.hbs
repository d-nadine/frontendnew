{{#each controller itemController="emailsItem" itemViewClass="Radium.EmailsItemView"}}
  {{partial "emails/item"}}
{{/each}}

{{#if hiddenContent}}
  <div id="remaining-emails">
    {{partial "shared/show_more"}}
  </div>
{{/if}}
