<div class="modal fade">
  <div class="modal-header">
    <button type="button" class="close" {{action "close"}} aria-hidden="true">&times;</button>
    <h3>Delete your account?  Really?</h3>
  </div>
  <div class="modal-body">
    {{#if beingDestroyed}}
      <img src="/images/ajax-loader.gif"/> <span class="muted">This might take a while...</span>
    {{/if}}
    <p>This action cannot be undone! Deleting your account will be permanent and unrecoverable.</p>

    <button class="btn btn-large btn-danger btn-block" {{action "destroyAccount"}} {{bind-attr disabled=beingDestroyed}}>I understand the consequences, delete my account.</button>
  </div>

  <div class="modal-footer">
    <a href="#" class="btn" {{action "close"}} {{bind-attr disabled=beingDestroyed}}>Cancel</a>
  </div>
</div>
