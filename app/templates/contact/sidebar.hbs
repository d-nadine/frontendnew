<form class="form">
  <div class="meta">
    {{avatar this size=medium class="img-polaroid"}}
    <p>
      <span class="type muted">contact</span><br/>
      <span class="name">{{name}}</span><br/>
      <span class="title muted">{{title}}</span>
      <span class="company">
        {{#if company}}
         {{#linkTo unimplemented}}{{company.name}}{{/linkTo}}
        {{/if}}
      </span>
    </p>
    <hr />
    <div class="contact-type">
      <div>
        <div class="type-label">
          <h2>Type</h2>
        </div>
        <div class="status">
          <div>{{view view.statusSelect}}</div>
        </div>
      </div>
    </div>
    <hr />
    <div class="company-website">
      {{#view view.contactInlineEditor valueBinding=company.website}}
        {{#if view.isEditing}}
          {{input value=company.website placeholder="Company Website"}}
        {{else}}
          <i class="icon-edit pull-right" {{action toggleEditor target=view bubbles=false}}></i>
          <div class="not-editing">
            <a href="mailto:{{unbound company.website}}">{{company.website}}</a>
          </div>
        {{/if}}
      {{/view}}
    </div>
    <div class="email">
      <div>
        <a href="mailto:{{unbound primaryEmail.value}}">{{primaryEmail.value}}</a>
      </div>
    </div>
    <div class="phone">
      <div>
        <div>
          {{primaryPhone.value}}
        </div>
        <div>
          <button class="btn btn-success">
            <i class="icon-call"></i>
          </button>
        </div>
      </div>
    </div>
    <div class="show-more">
      <a href="#" class="btn" {{action showMore bubbles=false target="view"}}>
        <i id="existingToggle" class="icon-arrow-down"></i>
      </a>
    </div>
    <hr/>
      <div class="group-picker">
        {{view view.groups placeholder="Add" viewName="tags"}}
        <div class="group-label">
          <h2><i class="icon-plus"></i> Group</h2>
        </div>
      </div>
    <hr/>
  </div>
</form>
