{{#if isCreate}}
  "{{resource-link model=attachment}}" uploaded to the {{resource-link attachedTo}} deal with {{resource-link model=company}}
{{/if}}

{{#if isUpdate}}
  "{{resource-link model=attachment}}" file changed on the {{resource-link model=attachedTo}} deal with {{resource-link model=company}}
{{/if}}

{{#if isDelete}}
  "{{resource-link model=attachment}}" file removed form the {{resource-link model=attachedTo}} deal with {{resource-link model=company}}
{{/if}}
