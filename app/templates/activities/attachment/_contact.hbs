{{#if isCreate}}
  "{{resource-link-to attachment}}" uploaded to {{resource-link-to attachedTo}}
{{/if}}

{{#if isUpdate}}
  "{{resource-link-to attachment}}" file changed for {{resource-link-to attachedTo}}
{{/if}}

{{#if isDelete}}
  "{{resource-link-to attachment}}" file removed form {{resource-link-to attachedTo}}
{{/if}}
