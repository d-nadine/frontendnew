{{#if isCreate}}
  "{{resource-link model=attachment}}" uploaded to {{resource-link model=attachedTo}}
{{/if}}

{{#if isUpdate}}
  "{{resource-link model=attachment}}" file changed for {{resource-link model=attachedTo}}
{{/if}}

{{#if isDelete}}
  "{{resource-link model=attachment}}" file removed form {{resource-link model=attachedTo}}
{{/if}}
