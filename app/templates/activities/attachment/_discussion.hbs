{{#if isCreate}}
  "{{link attachment}}" added to a {{#linkTo unimplemented}}discussion{{/linkTo}}
{{/if}}

{{#if isUpdate}}
  "{{link attachment}}" file changed for a {{#linkTo unimplemented}}discussion{{/linkTo}}
{{/if}}

{{#if isDelete}}
  "{{link attachment}}" file removed form a {{#linkTo unimplemented}}discussion{{/linkTo}}
{{/if}}
