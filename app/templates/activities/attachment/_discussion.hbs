{{#if isCreate}}
  "{{resource-link model=attachment}}" added to discussion about {{resource-link model=attachedTo}}
{{/if}}

{{#if isUpdate}}
  "{{resource-link model=attachment}}" file changed on the discussion about {{resource-link model=attachedTo}}
{{/if}}

{{#if isDelete}}
  "{{resource-link model=attachment}}" file removed form the discussion about {{resource-link model=attachedTo}}
{{/if}}
