<p>
  {{#if reference.description}}
    "{{reference.description}}" call added to {{resource-link-to contact}}
  {{else}}
    Call added to {{resource-link-to contact}}
  {{/if}}

  <br />

  by {{resource-link-to user}}
</p>
