<p>
  sent to {{resource-link-to contact}}
  <div class="email-subject">
    {{#if email.isLoaded}}
      {{resource-link-to email}}
    {{/if}}
  </div>
  <div class="email-body">
    {{{email.formattedEmailBody}}}
  </div>
</p>
