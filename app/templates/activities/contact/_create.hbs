{{#if hasStatus}}
  <p>
    {{link contact}} added as a {{status}}
    <br />
    by {{link user}}
  </p>
{{else}}
  <p>
    {{link contact}} added as a contact
    <br />
    by {{link user}}
  </p>
{{/if}}
