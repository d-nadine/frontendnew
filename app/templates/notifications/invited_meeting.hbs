{{#with view.reference.meeting}}
  {{#if isLoaded}}
    Meeting invitation for "{{topic}}"
    {{#if location}}
      @ {{location}}
    {{/if}}

    ({{formatDate startsAt}})
  {{else}}
    Loading...
  {{/if}}
{{/with}}
