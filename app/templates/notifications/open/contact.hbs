<h4>Your email <a href="#" {{action "showEmail" email}}>'{{email.displayName}}'</a> has been opened by
  {{#if reference.isLoaded}}
    {{resource-link-to reference}}
  {{else}}
    {{partial 'is_loading'}}
  {{/if}} on {{formatDateTime time format="full"}}.
</h4>