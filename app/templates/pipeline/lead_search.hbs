<ul class="unstyled" id="lead-search">
  <li>
    {{#each contact in content}}
      {{#with contact}}
        <a {{action showMessages this.name href=true }} class="thumbnail">
          {{avatar contact}}
          {{displayName}}
        </a>
      {{/with}}
    {{/each}}
  </li>
</ul>
