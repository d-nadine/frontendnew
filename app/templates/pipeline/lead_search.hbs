<ul class="unstyled" id="pipeline-search">
  <li>
    {{#each contact in content}}
      {{#with contact}}
        <a {{action showLeadSearch target="controller"}} class="thumbnail">
          {{avatar contact}}
          {{displayName}}
        </a>
      {{/with}}
    {{/each}}
  </li>
</ul>
