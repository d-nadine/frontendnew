{{#if showHeader}}
  {{partial 'pipeline/header'}}
{{/if}}
<table class="table table-hover pipeline-workflow-deals-list">
  <thead>
    <tr>
      <th class="checkbox">Select All</th>
      <th class="deal">Deal</th>
      <th class="assigned-to">Assigned To</th>
      <th class="pay-by">Pay By</th>
      <th class="next-task">Next Task</th>
      <th class="value">Value</th>
    </tr>
  </thead>
  <tbody>
    {{#each visibleContent itemController="pipelineItem"}}
      <tr {{bindAttr class="isChecked"}}>
        <td class="checkbox">
          {{check-box checked=isChecked class="checker-block"}}
        </td>
        <td class="deal">
          <p>
            <span class="name">{{#linkTo "deal" this}}{{name}}{{/linkTo}}</span><br/>
            {{#linkTo "contact" contact}}{{contact.displayName}}{{/linkTo}}<br/>
            {{contact.title}}
          </p>
        </td>
        <td class="assigned-to">
          <p>
            <span class="name">{{#linkTo "user" user}}{{user.name}}{{/linkTo}}</span></br>
            <span class="title">{{user.title}}</span>
          <p>
        </td>
        <td {{bindAttr  class=":pay-by isPastPayment"}}>
          {{formatDateTime payBy format=date}}
        </td>
        <td class="next-task">
          {{partial 'pipeline/pipeline_next_task'}}
        </td>
        <td class="value">{{currency value}}</td>
      </tr>
    {{else}}
      <tr>
        <td colspan="7">No Deals</td>
      </tr>
    {{/each}}
  </tbody>
</table>

{{partial "shared/show_more"}}
{{partial "pipeline/totals"}}
