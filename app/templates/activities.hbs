{{#each activity in controller}}
  {{#if activity.isLoaded}}
    {{#unless activity.reference.isDeleted}}
      {{renderActivity}}
    {{/unless}}
  {{else}}
    {{partial 'is_loading'}}
  {{/if}}
{{/each}}
