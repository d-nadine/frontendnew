{{#if todo.canEdit}}
  {{view checkboxView}}
{{/if}}
{{#if todo.reference.contact}}
 {{view Radium.LabelView labelBinding="todo.reference.contact.status"}}
{{/if}}
<span class="content">
  {{#unless todo.isCallList}}
    {{#if todo.canEdit}}
      {{inlineTextField todo.description}}
    {{else}}
      {{todo.description}}
    {{/if}}
  {{/unless}}


  {{#if todo.isCallList}}
    {{#if todo.isSaving}}Assigning...{{/if}}
    {{#if todo.description}}
      {{todo.description}} for <a href="/contacts/{{unbound todo.reference.contact.id}}">{{todo.reference.contact.name}}</a> 
    {{else}}
      Call <a href="/contacts/{{unbound todo.reference.contact.id}}">{{todo.reference.contact.name}}</a> 
    {{/if}}
  {{/if}}
  
  (<a href="/users/{{unbound todo.user.id}}">{{todo.user.abbrName}}</a>)
</span>