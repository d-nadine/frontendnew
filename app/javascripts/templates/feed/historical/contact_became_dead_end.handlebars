{{#with contact}}
  <i class="pull-left activity-icon icon-contact"></i>
  <span class="label label-inverse">DEAD END</span> {{name}} became a dead end (<a {{bindAttr href="user.url"}}>{{user.abbrName}}</a>)
{{/with}}