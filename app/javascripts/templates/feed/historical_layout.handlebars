{{#view feedHeaderView class="row"}}
  <div class="span9 feed-header">
    <div class="row">
      <div class="span9 ">
        {{view iconView}}
        <p>
          {{#with content}}
            <span class="content">{{yield}}</span>
            {{#if comments.length}}
              <i class="icon-comment"></i>
              ({{comments.length}})
            {{/if}}
          {{/with}}
        </p>
      </div> <!-- /.span9 -->
    </div> <!-- /.row -->
    {{#if isActionsVisible}}
      <p class="feed-actions">
        <a href="#" {{action "addTodo" target="parentView"}}><i class="icon-check"></i> Add Todo</a>
      {{#if content.reference.contact}}
        <a href="#" {{action "addCallTask" target="parentView"}}><i class="icon-check"></i> Add Call Todo</a>
      {{/if}}
      </p>
    {{/if}}
  </div> <!-- /.feed-header -->
{{/view}}
{{view activityActionsView}}