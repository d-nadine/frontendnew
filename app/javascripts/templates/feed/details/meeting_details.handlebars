<dl class="dl-horizontal">
{{#if users.length}}
  <dt>Users:</dt>
  <dd>{{#each users}}{{name}} {{/each}}</dd>
{{/if}}
{{#if contacts.length}}
  <dt>Contacts:</dt>
  <dd>{{#each contacts}}{{name}} {{/each}}</dd>
{{/if}}
</dl>