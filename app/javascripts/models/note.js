/**
  Note: Discussions are labeled in views as Notes. Code refers to them as
  Notes so there is no confusion dealing with the API.
*/
Radium.Note = Radium.Core.extend({
  message: DS.attr('string')
});