define('fixtures/main', function(require) {
  require('./users');
  require('./contacts');
  require('./activities');
  require('./todos');
  require('./activity_date_groups');
  require('./announcements');

});