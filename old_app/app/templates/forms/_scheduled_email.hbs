{{#if isScheduled }}
  <div class="alert alert-success">
    Email scheduled for delivery at {{sendTimeFormatted}}.  Click <a href="#" {{action "cancelDelivery" form}}>this</a> to cancel.
  </div>
{{/if}}
