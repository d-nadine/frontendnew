{{#each activity in activities}}
  {{#with activity}}
    {{#if activity.isLoaded}}
      {{#unless activity.reference.isDeleted}}
        {{renderActivity "activity"}}
      {{/unless}}
    {{else}}
      {{partial 'is_loading'}}
    {{/if}}
  {{/with}}
{{/each}}
