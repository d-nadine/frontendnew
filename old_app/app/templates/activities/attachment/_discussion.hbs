{{#if isCreate}}
  "{{resource-link-to attachment}}" added to discussion about {{resource-link-to attachedTo}}
{{/if}}

{{#if isUpdate}}
  "{{resource-link-to attachment}}" file changed on the discussion about {{resource-link-to attachedTo}}
{{/if}}

{{#if isDelete}}
  "{{resource-link-to attachment}}" file removed form the discussion about {{resource-link-to attachedTo}}
{{/if}}
